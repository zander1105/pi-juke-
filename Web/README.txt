To run the webserver, 

run the following command in this directory ("Web") (or the equivalent command to run Python scripts for your OS)

$ python3 server.py

Then in a browser, load up 

http://localhost:8123/htbin/index.py

This will display a message on the screen and write a text file into the top level Web directory